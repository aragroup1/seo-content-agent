{
  "name": "seo-content-agent",
  "version": "1.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "cd backend && pip install -r requirements.txt && python main.py", // <--- THIS IS THE PROBLEM
    "lint": "next lint"
  },
}
